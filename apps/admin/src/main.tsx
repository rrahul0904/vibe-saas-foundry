import React,{useEffect,useState} from "react";import{createRoot}from"react-dom/client";import"@foundry/design-tokens/tokens.css";import"./styles.css";
const API=import.meta.env.VITE_API_URL||"http://localhost:4000";
function App(){const[key,setKey]=useState(localStorage.getItem("foundry_admin_key")||"change-me"),[metrics,setMetrics]=useState<any>(null),[users,setUsers]=useState<any[]>([]),[error,setError]=useState("");async function load(){localStorage.setItem("foundry_admin_key",key);try{setError("");const h={"x-admin-key":key};const[m,u]=await Promise.all([fetch(`${API}/admin/metrics`,{headers:h}),fetch(`${API}/admin/users`,{headers:h})]);if(!m.ok||!u.ok)throw new Error("Admin API rejected the key");setMetrics(await m.json());setUsers(await u.json())}catch(e:any){setError(e.message)}}useEffect(()=>{load()},[]);return <main><header><div><span>FOUNDRY OPS</span><h1>Operator console</h1><p>Separate admin surface for system visibility without leaking operator concerns into the customer app.</p></div><div className="key"><input value={key} onChange={e=>setKey(e.target.value)}/><button onClick={load}>Refresh</button></div></header>{error&&<div className="error">{error}</div>}<section className="metrics">{["users","tasks","sessions"].map(k=><article key={k}><b>{metrics?.[k]??"—"}</b><span>{k}</span></article>)}</section><section className="panel"><div className="panelHead"><h2>Recent users</h2><span>latest 20</span></div><table><thead><tr><th>Email</th><th>Verified</th><th>Created</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.email}</td><td>{u.verified?"Yes":"No"}</td><td>{new Date(u.createdAt).toLocaleString()}</td></tr>)}</tbody></table></section></main>}createRoot(document.getElementById("root")!).render(<App/>);
